# RailVision AI — desktop app dependencies
# Install with:  pip install -r desktop/requirements.txt
#
# The desktop app loads the backend from source (desktop/__init__.py adds the
# ../backend dir to sys.path), so you do NOT need `pip install -e backend`.
# These packages are everything the desktop app imports at runtime.

PyQt6>=6.6
numpy>=1.26
opencv-python>=4.9           # full build (GUI codecs) for webcam/file capture
pydantic>=2.7
pydantic-settings>=2.3
structlog>=24.1

# --- Optional ---
pyttsx3>=2.90                # offline voice alerts (omit for silent mode)
# ultralytics>=8.3           # real YOLO26 inference (else mock fallback)
# torch>=2.2                 # GPU acceleration
